const keyboardButtons = require('./keyboardButtons')
const KeyboardOptions = require('./keyboardButtons')

module.exports = {
    Home: [
        [keyboardButtons.Home.functions, keyboardButtons.Home.inequalities],
        [keyboardButtons.Home.back]
    ],
    Theme: [
        
    ]
}